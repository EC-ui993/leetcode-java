---
id: lc-0000
title: 中文题名
category: 01-array-hash
tags: [标签一, 标签二]
difficulty: Easy
status: 独立完成
url: https://leetcode.cn/problems/example-slug/
date: 2026-01-01
---

<!--
本文件是 tools/new-problem.mjs 的【唯一模板来源】，脚本内不得内嵌第二份副本。

⚠️ 本文件里的 HTML 注释（也就是读到这一段）只用于说明规范，
   脚手架生成笔记时会整段剥离，不会出现在任何题目的 README 里。
   所以规范说明写在这里、紧挨着它描述的内容；而正文里
   `（用自己的话写：……）` 这类提示是**普通文本**，会保留在生成的笔记里待填写。

front-matter 规范（刻意限制，不遵守会被脚本拒绝）：
  - 只允许扁平键值 `key: value` 与单行数组 `key: [a, b]`
  - 不允许嵌套、不允许 `|` / `>` 多行字符串
  - 这样零依赖解析器（约 40 行）即可可靠工作，仓库无需 node_modules

schema:
  id          必填  lc-<四位题号>，全局唯一
  title       必填  中文题名（目录名另用英文 slug，见 URL）
  category    必填  categories.yml 中的合法 id，且必须与所在目录一致
  tags        必填  至少 1 个，必须在 categories.yml 的标签词表内
  difficulty  必填  Easy / Medium / Hard
  status      必填  独立完成 / 看题解完成 / 未通过
  url         必填  LeetCode 链接（用它替代题面，见下方版权说明）
  date        必填  该次 AC 的日期 YYYY-MM-DD

入库准则（很重要）：只有提交到 LeetCode 并通过（AC）的代码才允许入库。
正确性由 LeetCode 判定，本仓库只记录结论。未 AC 的必须如实标 `未通过`。

版权：不要整段复制 LeetCode 的题目描述。只写自己的思路 + 关键约束 + 原题链接。
-->

# {{id}} {{title}}

## 思路

（用自己的话写：为什么想到这个做法？瓶颈在哪？）

## 复杂度

- 时间：O(?)
- 空间：O(?)

## 解法演进

（从暴力到最优：每一步换来了什么？卡在哪里？）

## 踩坑

（当时错了几次、错在哪、有什么反直觉的点？）

## 关联题

（同类题、可迁移的模板、值得举一反三的地方）
