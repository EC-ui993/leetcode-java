# 仓库内一律使用 LF，杜绝 Windows CRLF 造成的整文件假 diff
* text=auto eol=lf

# 显式声明文本类型，避免被误判
*.java  text eol=lf
*.md    text eol=lf
*.yml   text eol=lf
*.mjs   text eol=lf

# 二进制文件不做换行符转换
*.png   binary
*.jpg   binary
*.pdf   binary
